'use strict'

// 15. Write a javascirpt function to compute the value of bn Where nis the exponent and b is the bases. Accept b and n from the user and display the result.



const exponent = (b,n) =>{
    return Math.pow(b,n);
}

console.log(exponent(3,5));
console.log(exponent(2,3));